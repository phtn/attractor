// import { createHTTPServer } from "@trpc/server/adapters/standalone";
// import { proc, r } from "./trpc";

// const router = r({
//   getUser: proc.query(async ({ input }) => input),
// });
// export type AppRouter = typeof router;

// const server = createHTTPServer({
//   router,
// });

// server.listen(3000);
